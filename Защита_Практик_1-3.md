# Руководство по защите практических работ 1-3

## Общие рекомендации по защите

- **Будьте уверены**: Знайте свой код и будьте готовы объяснить любую строку
- **Говорите четко**: Используйте правильную терминологию
- **Показывайте понимание**: Объясняйте не только "что", но и "почему"
- **Демонстрируйте работу**: Запустите программы и покажите результаты

---

## Практика 1: Основы ООП в Java

### Что демонстрирует эта практика

1. **Создание классов** (`Car.java`)
2. **Перегрузка конструкторов** (Constructor Overloading)
3. **Инкапсуляция** (Getters и Setters)
4. **Методы класса**
5. **Работа с пользовательским вводом** (`Scanner`)
6. **Форматированный вывод** (`printf`)

### Ключевые концепции для объяснения

#### 1. Класс Car (src/practice1/Car.java)

**Поля класса:**
```java
String model;      // Модель автомобиля
String license;    // Номерной знак
String color;      // Цвет
int year;          // Год выпуска
```

**Три конструктора (перегрузка):**
- `Car(String mod, String cl, String lc, int y)` - полная инициализация
- `Car(String lc)` - инициализация только номера
- `Car()` - конструктор по умолчанию

**Важно объяснить:**
- Зачем нужна перегрузка конструкторов?
  - *Ответ:* Для гибкости создания объектов с разным набором параметров

#### 2. Методы

**Getters и Setters:**
- Зачем они нужны?
  - *Ответ:* Для обеспечения инкапсуляции и контроля доступа к полям

**Метод `calcAge(int currentYear)`:**
- Вычисляет возраст автомобиля
- Использует ключевое слово `this` для обращения к полю класса

#### 3. YuanToRoubles (src/practice1/YuanToRoubles.java)

**Демонстрирует:**
- Константы (`final double ROUBLES_PER_YUAN`)
- Условные операторы (if-else)
- Работу с модулем (`%`) для грамматики
- Приведение типов (`(int)Math.round()`)

### Вопросы, которые могут задать

**В:** Почему поля класса Car не private?
**О:** В учебных целях для простоты, но в production-коде лучше использовать private с геттерами/сеттерами.

**В:** Что такое `this` и зачем оно нужно?
**О:** `this` - ссылка на текущий объект. Используется для различения полей класса от параметров методов с одинаковыми именами.

**В:** Объясните перегрузку конструкторов.
**О:** Это возможность создать несколько конструкторов с разным количеством или типом параметров для гибкого создания объектов.

### Как запустить и продемонстрировать

```bash
# Компиляция
javac src/practice1/Car.java src/practice1/Cars.java

# Запуск
java -cp src practice1.Cars

# Для YuanToRoubles
javac src/practice1/YuanToRoubles.java
java -cp src practice1.YuanToRoubles
```

### Пример демонстрации Cars.java

1. Показать создание объекта с начальными данными (Tiguan, Silver, B456KX177, 2018)
2. Показать изменение полей через setters
3. Показать вывод информации через To_String()
4. Показать вычисление возраста автомобиля

---

## Практика 2: Наследование и полиморфизм

### Что демонстрирует эта практика

1. **Абстрактные классы** (`abstract class Vehicle`)
2. **Наследование** (`extends`)
3. **Модификаторы доступа** (`private`, `protected`, `public`)
4. **Абстрактные методы**
5. **Переопределение методов** (`@Override`)
6. **Полиморфизм** (Vehicle может ссылаться на Car или ElectricCar)
7. **Приведение типов** (Casting)

### Ключевые концепции для объяснения

#### 1. Абстрактный класс Vehicle

**Почему abstract?**
- Нельзя создать экземпляр Vehicle напрямую
- Служит базой для других классов
- Определяет общие поля и методы для всех транспортных средств

**Поля:**
- `private` поля: model, color, license, year, ownerName, insuranceNumber
- `protected` поле: engineType (доступно наследникам)

**Абстрактный метод:**
```java
abstract String vehicleType();
```
- Должен быть реализован в каждом наследнике

#### 2. Класс Car extends Vehicle

**Наследование:**
```java
public class Car extends Vehicle
```

**Конструктор:**
- Использует `super()` для вызова конструктора родителя
- Передает все параметры в родительский класс

**Переопределение:**
```java
@Override
String vehicleType() {
    return "Car";
}
```

#### 3. Класс ElectricCar extends Car

**Множественное наследование:**
- ElectricCar → Car → Vehicle (цепочка наследования)

**Дополнительное поле:**
- `batteryCapacity` - емкость батареи

**Переопределение в конструкторе:**
```java
this.engineType = "Electric";
```

**Переопределение toString():**
- Вызывает `super.toString()` и добавляет информацию о батарее

### Вопросы, которые могут задать

**В:** В чем разница между abstract class и обычным классом?
**О:**
- Нельзя создать экземпляр абстрактного класса
- Может содержать абстрактные методы (без реализации)
- Служит базой для наследования

**В:** Зачем нужен модификатор protected?
**О:** Поле доступно в классе и всех его наследниках, но не снаружи.

**В:** Что такое полиморфизм? Покажите в коде.
**О:**
```java
Vehicle passat = new Car(...);  // Vehicle может ссылаться на Car
Vehicle spectre = new ElectricCar(...);  // или на ElectricCar
```

**В:** Зачем нужен @Override?
**О:**
- Явно указывает, что метод переопределяет родительский
- Компилятор проверит правильность переопределения
- Улучшает читаемость кода

**В:** Что делает super()?
**О:** Вызывает конструктор родительского класса для инициализации его полей.

**В:** Что такое приведение типов (casting)?
**О:**
```java
((ElectricCar) spectre).setBatteryCapacity(105);
```
Преобразует тип Vehicle в ElectricCar для доступа к специфичным методам.

### Как запустить и продемонстрировать

```bash
# Компиляция всех файлов
javac src/practice2/vehicles/*.java src/practice2/app/*.java

# Запуск
java -cp src practice2.app.TestCar
```

### Демонстрация работы

1. Создание объекта Car через ссылку Vehicle (полиморфизм)
2. Создание объекта ElectricCar через ссылку Vehicle
3. Изменение полей через setters
4. Использование casting для доступа к методам ElectricCar
5. Показать вывод toString() для обоих объектов

---

## Практика 3: Интерфейсы и расширенное наследование

### Что демонстрирует эта практика

1. **Интерфейсы** (`interface ElectricVehicle`)
2. **Множественное наследование** (класс + интерфейс)
3. **Реализация интерфейсов** (`implements`)
4. **Комбинация extends и implements**
5. **Работа с велосипедами** (дополнительные классы Bike)

### Ключевые концепции для объяснения

#### 1. Интерфейс ElectricVehicle

```java
public interface ElectricVehicle {
    int getBatteryCapacity();
    void setBatteryCapacity(int batteryCapacity);
}
```

**Особенности интерфейса:**
- Все методы публичные и абстрактные (по умолчанию)
- Нет реализации методов (только сигнатуры)
- Класс может реализовывать несколько интерфейсов

#### 2. Класс ElectricCar

```java
public class ElectricCar extends Car implements ElectricVehicle
```

**Демонстрирует:**
- Наследование от Car (`extends Car`)
- Реализацию интерфейса (`implements ElectricVehicle`)
- Java не поддерживает множественное наследование классов, но можно реализовать несколько интерфейсов

**Реализация методов интерфейса:**
```java
@Override
public int getBatteryCapacity() {
    return batteryCapacity;
}

@Override
public void setBatteryCapacity(int batteryCapacity) {
    this.batteryCapacity = batteryCapacity;
}
```

#### 3. Иерархия классов Bike

**Bike** (базовый класс)
- Базовые характеристики велосипеда

**BikeParts** (интерфейс)
- Определяет части велосипеда

**RoadBike и MountainBike**
- Специализированные велосипеды
- Реализуют свои интерфейсы (RoadParts, MountainParts)

### Вопросы, которые могут задать

**В:** В чем разница между абстрактным классом и интерфейсом?
**О:**
- **Интерфейс:**
  - Только сигнатуры методов (до Java 8)
  - Класс может реализовывать много интерфейсов
  - Используется для определения контракта
- **Абстрактный класс:**
  - Может иметь реализацию методов
  - Может иметь поля
  - Только одиночное наследование

**В:** Почему ElectricCar extends Car implements ElectricVehicle?
**О:**
- `extends Car` - наследует функциональность автомобиля
- `implements ElectricVehicle` - добавляет контракт электромобиля
- Это позволяет иметь и поведение Car, и функции электромобиля

**В:** Можно ли implements несколько интерфейсов?
**О:**
Да!
```java
class MyClass implements Interface1, Interface2, Interface3
```

**В:** Зачем нужны интерфейсы?
**О:**
- Определяют контракт (что должен уметь класс)
- Позволяют множественное "наследование" поведения
- Обеспечивают полиморфизм
- Слабая связанность (loose coupling)

**В:** Что означает @Override над методом интерфейса?
**О:** Показывает, что метод реализует метод из интерфейса или переопределяет родительский метод.

### Как запустить и продемонстрировать

```bash
# Компиляция
javac src/practice3/vehicles/*.java src/practice3/app/*.java
javac src/practice3/bikeproject/*.java

# Запуск TestCar
java -cp src practice3.app.TestCar

# Запуск BikeDriver
java -cp src practice3.bikeproject.BikeDriver
```

### Демонстрация работы

#### TestCar:
1. Создание объектов через полиморфизм (Vehicle → Car, ElectricCar)
2. Изменение полей
3. Использование методов интерфейса (getBatteryCapacity, setBatteryCapacity)
4. Показать, что ElectricCar одновременно и Car, и ElectricVehicle

#### BikeDriver:
1. Создание разных типов велосипедов
2. Конструкторы с разными параметрами
3. Метод printDescription() для каждого типа
4. Изменение параметров через setters

---

## Общие вопросы по всем трем практикам

### 1. ООП принципы

**В:** Какие принципы ООП используются в работах?
**О:**
- **Инкапсуляция** - private поля, public геттеры/сеттеры
- **Наследование** - Car extends Vehicle
- **Полиморфизм** - Vehicle может ссылаться на Car или ElectricCar
- **Абстракция** - abstract class Vehicle, интерфейсы

### 2. Модификаторы доступа

**В:** Объясните модификаторы доступа.
**О:**
- **private** - доступ только внутри класса
- **protected** - доступ в классе и наследниках
- **public** - доступ отовсюду
- **package-private** (без модификатора) - доступ в пределах пакета

### 3. Конструкторы

**В:** Что такое конструктор?
**О:** Специальный метод для инициализации объекта при создании. Имя совпадает с именем класса, не имеет возвращаемого типа.

### 4. this и super

**В:** Разница между this и super?
**О:**
- **this** - ссылка на текущий объект
- **super** - ссылка на родительский класс

### 5. Переопределение методов

**В:** Что такое переопределение (overriding)?
**О:** Изменение реализации метода родительского класса в наследнике. Сигнатура остается той же.

---

## Практические советы для защиты

### Подготовка

1. **Запустите все программы заранее** и убедитесь, что они работают
2. **Знайте, где находится каждый файл** в структуре проекта
3. **Подготовьте примеры ввода** для интерактивных программ
4. **Повторите теорию** ООП (инкапсуляция, наследование, полиморфизм)

### Во время защиты

1. **Начните с общего описания** задачи
2. **Покажите структуру проекта** (packages, классы)
3. **Объясните иерархию классов** (можно нарисовать диаграмму)
4. **Запустите программу** и покажите результат
5. **Прокомментируйте ключевые моменты** в коде

### Типичные ошибки, которых следует избегать

❌ "Я не помню, зачем это нужно"
✅ Всегда объясняйте цель каждой части кода

❌ "Код просто работает"
✅ Объясните, КАК и ПОЧЕМУ он работает

❌ Путаница в терминах
✅ Используйте правильные термины: метод, поле, конструктор, класс

### Диаграмма наследования для практик 2-3

```
Practice 2:
    Vehicle (abstract)
       ↑
       |
      Car
       ↑
       |
   ElectricCar

Practice 3:
    Vehicle (abstract)          ElectricVehicle (interface)
       ↑                                 ↑
       |                                 |
      Car                                |
       ↑                                 |
       |                                 |
   ElectricCar -------------------------+
              (extends Car, implements ElectricVehicle)
```

---

## Примеры ответов на вопросы

### "Зачем нужна инкапсуляция?"

*Хороший ответ:*
"Инкапсуляция скрывает внутреннюю реализацию класса и предоставляет контролируемый доступ к данным через геттеры и сеттеры. Это позволяет:
- Защитить данные от некорректного изменения
- Изменить внутреннюю реализацию без изменения внешнего API
- Добавить валидацию при изменении полей"

### "В чем преимущество полиморфизма?"

*Хороший ответ:*
"Полиморфизм позволяет работать с объектами разных типов через общий интерфейс. В моем коде:
```java
Vehicle car1 = new Car(...);
Vehicle car2 = new ElectricCar(...);
```
Обе переменные имеют тип Vehicle, но ссылаются на разные объекты. Это упрощает код и делает его более гибким."

### "Почему Vehicle абстрактный?"

*Хороший ответ:*
"Vehicle - это абстрактная концепция. Не существует просто 'транспортного средства' - всегда есть конкретный тип: автомобиль, велосипед и т.д. Абстрактный класс:
- Не позволяет создать объект Vehicle напрямую
- Обязывает наследников реализовать метод vehicleType()
- Служит шаблоном для всех транспортных средств"

---

## Команды для быстрого запуска

```bash
# Practice 1 - Cars
javac src/practice1/*.java && java -cp src practice1.Cars

# Practice 1 - YuanToRoubles
javac src/practice1/YuanToRoubles.java && java -cp src practice1.YuanToRoubles

# Practice 2
javac src/practice2/vehicles/*.java src/practice2/app/*.java && java -cp src practice2.app.TestCar

# Practice 3 - Vehicles
javac src/practice3/vehicles/*.java src/practice3/app/*.java && java -cp src practice3.app.TestCar

# Practice 3 - Bikes
javac src/practice3/bikeproject/*.java && java -cp src practice3.bikeproject.BikeDriver
```

---

## Финальный чек-лист перед защитой

- [ ] Все программы компилируются без ошибок
- [ ] Все программы запускаются и работают корректно
- [ ] Знаю расположение всех файлов в проекте
- [ ] Могу объяснить каждую строку кода
- [ ] Понимаю разницу между классом, абстрактным классом и интерфейсом
- [ ] Могу объяснить принципы ООП на примере своего кода
- [ ] Знаю, что такое конструктор, метод, поле
- [ ] Понимаю модификаторы доступа
- [ ] Могу объяснить наследование и полиморфизм
- [ ] Готов ответить на вопросы по теории

---

**Удачи на защите! 🚀**

Помните: уверенность приходит с пониманием. Если вы понимаете, что и зачем делает ваш код, защита пройдет успешно.
