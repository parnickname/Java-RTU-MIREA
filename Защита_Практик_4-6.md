# Руководство по защите практических работ 4-6

## Общие рекомендации по защите

- **Будьте уверены**: Знайте свой код и будьте готовы объяснить любую строку
- **Говорите четко**: Используйте правильную терминологию
- **Показывайте понимание**: Объясняйте не только "что", но и "почему"
- **Демонстрируйте работу**: Запустите программы и покажите результаты

---

## Практика 4: ООП в Java. Исключения

### Что демонстрирует эта практика

1. **Наследование** (Triangle extends GeometricObject)
2. **Абстрактные классы и методы**
3. **Интерфейс Comparable** и сравнение объектов
4. **Пользовательские исключения** (IllegalTriangleException)
5. **Обработка исключений** (try-catch)
6. **Интерфейс Colorable**
7. **Статические методы** (max в GeometricObject)

### Ключевые концепции для объяснения

#### 1. Абстрактный класс GeometricObject (src/practice4/figures/geometry/GeometricObject.java)

**Поля класса:**
```java
private String color = "белый";
private boolean filled;
private java.util.Date dateCreated;
```

**Абстрактные методы:**
```java
abstract public double getArea();
abstract public double getPerimeter();
```

**Важно объяснить:**
- Почему класс абстрактный?
  - *Ответ:* GeometricObject - абстрактная концепция. Не существует "просто фигуры" - всегда есть конкретный тип (круг, треугольник, квадрат). Абстрактные методы getArea() и getPerimeter() обязывают каждую фигуру реализовать вычисление площади и периметра.

**Интерфейс Comparable:**
```java
public class GeometricObject implements Comparable<GeometricObject>
```

**Метод compareTo:**
```java
@Override
public int compareTo(GeometricObject o) {
    return Double.compare(this.getArea(), o.getArea());
}
```
- Сравнивает фигуры по площади
- Использует Double.compare() для корректного сравнения вещественных чисел
- Возвращает: отрицательное число (this < o), 0 (равны), положительное (this > o)

**Статический метод max:**
```java
public static GeometricObject max(GeometricObject obj1, GeometricObject obj2) {
    return obj1.compareTo(obj2) >= 0 ? obj1 : obj2;
}
```
- Статический метод - принадлежит классу, а не объекту
- Возвращает фигуру с большей площадью

#### 2. Класс Triangle (src/practice4/figures/geometry/Triangle.java:5-31)

**Поля класса:**
```java
double side1 = 1.0;
double side2 = 1.0;
double side3 = 1.0;
```

**Конструкторы с валидацией:**
```java
public Triangle(double side1, double side2, double side3) throws IllegalTriangleException {
    validateSides(side1, side2, side3);
    this.side1 = side1;
    this.side2 = side2;
    this.side3 = side3;
}
```

**Метод валидации:**
```java
private void validateSides(double side1, double side2, double side3)
        throws IllegalTriangleException {
    if (side1 + side2 <= side3 || side2 + side3 <= side1 || side1 + side3 <= side2) {
        throw new IllegalTriangleException(
            "The triangle sides are not valid: " + side1 + ", " + side2 + ", " + side3
        );
    }
}
```
- Проверяет правило треугольника: сумма любых двух сторон должна быть больше третьей
- Выбрасывает исключение при нарушении

**Конструктор с цепочкой вызовов:**
```java
public Triangle(double side1, double side2, double side3, String color, boolean filled)
        throws IllegalTriangleException {
    this(side1, side2, side3);  // Вызов другого конструктора
    setColor(color);
    setFilled(filled);
}
```

**Вычисление площади (формула Герона):**
```java
public double getArea() {
    double p = getPerimeter() / 2.0;  // Полупериметр
    return Math.sqrt(p * (p - side1) * (p - side2) * (p - side3));
}
```

#### 3. Пользовательское исключение IllegalTriangleException (src/practice4/figures/exceptions/IllegalTriangleException.java)

```java
public class IllegalTriangleException extends Exception {
    public IllegalTriangleException(String message) {
        super(message);
    }
}
```

**Важно понимать:**
- Наследуется от Exception (проверяемое исключение)
- Конструктор принимает сообщение об ошибке
- Должно быть объявлено в сигнатуре метода (throws)
- Должно быть обработано (try-catch) или пробрасываться дальше

#### 4. Интерфейс Colorable (src/practice4/figures/geometry/Colorable.java)

```java
public interface Colorable {
    void howToColor();
}
```

**Класс Square реализует Colorable:**
```java
public class Square extends GeometricObject implements Colorable {
    @Override
    public void howToColor() {
        System.out.println("Раскрасьте все четыре стороны");
    }
}
```

#### 5. Класс Loan с валидацией (src/practice4/secondPart/Loan.java:31-62)

**Сеттеры с проверками:**
```java
public void setAnnualInterestRate(double annualInterestRate) {
    if (annualInterestRate <= 0) {
        throw new IllegalArgumentException("процентная ставка должна быть больше нуля");
    }
    this.annualInterestRate = annualInterestRate;
}
```

**Почему IllegalArgumentException?**
- RuntimeException (непроверяемое исключение)
- Не требует объявления в throws
- Используется для валидации параметров

#### 6. MonthsAndDays с обработкой исключений (src/practice4/secondPart/MonthsAndDays.java:8-36)

**Метод проверки високосного года:**
```java
private static boolean isLeapYear(int year) {
    return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
}
```

**Обработка исключений:**
```java
try {
    int n = sc.nextInt();
    int days = dom[n - 1];
    // ...
} catch (ArrayIndexOutOfBoundsException e) {
    System.out.println("Ошибка: число должно быть в диапазоне от 1 до 12");
} catch (InputMismatchException e) {
    System.out.println("Ошибка: число должно быть целым");
}
```

### Вопросы, которые могут задать

**В:** Что такое checked и unchecked исключения?
**О:**
- **Checked (проверяемые)**: Exception и его подклассы (кроме RuntimeException). Компилятор требует их обработки. Пример: IllegalTriangleException
- **Unchecked (непроверяемые)**: RuntimeException и его подклассы, Error. Не требуют обязательной обработки. Пример: IllegalArgumentException, ArrayIndexOutOfBoundsException

**В:** Зачем нужны собственные исключения?
**О:** Для создания специфичных для приложения исключений с понятным именем и сообщением. IllegalTriangleException четко показывает, что ошибка связана с треугольником.

**В:** В чем разница между throw и throws?
**О:**
- **throw** - выбрасывает исключение: `throw new IllegalTriangleException(...)`
- **throws** - объявляет, что метод может выбросить исключение: `public Triangle(...) throws IllegalTriangleException`

**В:** Как работает интерфейс Comparable?
**О:** Определяет естественный порядок объектов. Метод compareTo() сравнивает текущий объект с другим. В нашем случае сравниваем по площади.

**В:** Почему используется Double.compare() вместо прямого сравнения?
**О:** Double.compare() корректно обрабатывает специальные значения (NaN, Infinity) и проблемы точности вещественных чисел.

### Как запустить и продемонстрировать

```bash
# Компиляция геометрических фигур
javac src/practice4/figures/geometry/*.java src/practice4/figures/exceptions/*.java src/practice4/figures/app/*.java

# Запуск тестов
java -cp src practice4.figures.app.TestTriangle
java -cp src practice4.figures.app.TestCompare
java -cp src practice4.figures.app.TestColorable

# Компиляция и запуск второй части
javac src/practice4/secondPart/*.java
java -cp src practice4.secondPart.MonthsAndDays
java -cp src practice4.secondPart.TestLoanClass
```

### Демонстрация работы

**TestTriangle:**
1. Создание валидного треугольника
2. Попытка создать невалидный треугольник (обработка исключения)
3. Вычисление площади и периметра

**TestCompare:**
1. Сравнение кругов и прямоугольников
2. Использование метода max()
3. Демонстрация ComparableCircle

**TestColorable:**
1. Создание массива GeometricObject
2. Проверка instanceof Colorable
3. Вызов howToColor() для раскрашиваемых объектов

---

## Практика 5: Дженерики и структуры данных. Стек

### Что демонстрирует эта практика

1. **Дженерики (Generics)** - параметризованные типы
2. **Generic методы** - методы с параметрами типа
3. **Структура данных Стек** (LIFO)
4. **Композиция vs Наследование**
5. **ArrayList и его методы**
6. **Интерфейс Comparable с дженериками**

### Ключевые концепции для объяснения

#### 1. Дженерики - что это и зачем?

**До дженериков (Java < 1.5):**
```java
ArrayList list = new ArrayList();
list.add("Hello");
String s = (String) list.get(0);  // Нужно приведение типа
```

**С дженериками:**
```java
ArrayList<String> list = new ArrayList<>();
list.add("Hello");
String s = list.get(0);  // Приведение не нужно
```

**Преимущества:**
- Безопасность типов на этапе компиляции
- Исключение ClassCastException
- Не нужно приведение типов

#### 2. Generic методы (src/practice5/ArraysAndCircle/DeleteDuplicatesAndLinearSearch.java:6-8)

**Линейный поиск:**
```java
static <T> int linearSearch(ArrayList<T> list, T target) {
    return list.indexOf(target);
}
```
- `<T>` - параметр типа (может быть любой тип)
- Метод работает с любым типом данных

**Удаление дубликатов:**
```java
static <T> ArrayList<T> deleteDuplicates(ArrayList<T> list) {
    ArrayList<T> result = new ArrayList<>();
    for (T item : list) {
        if (!result.contains(item)) {
            result.add(item);
        }
    }
    return result;
}
```

#### 3. Generic класс GenericStack1 (src/practice5/GenericStack/GenericStack1.java)

**Объявление:**
```java
public class GenericStack1<E> {
    private E[] arr;
    private int top;
}
```

**Работа с массивом:**
```java
public void push(E o) {
    if (top == arr.length - 1) {
        arr = Arrays.copyOf(arr, arr.length * 2);  // Увеличение размера
    }
    arr[++top] = o;
}
```

**Методы стека:**
```java
public E pop() {
    return arr[top--];  // Возвращает и удаляет верхний элемент
}

public E peek() {
    return arr[top];  // Только возвращает верхний элемент
}

public boolean isEmpty() {
    return top < 0;
}
```

**ToString с StringBuilder (src/practice5/GenericStack/GenericStack1.java:38-44):**
```java
@Override
public String toString() {
    StringBuilder sb = new StringBuilder();
    for (int i = 0; i <= top; i++) {
        sb.append(arr[i]).append(" ");
    }
    return sb.toString();
}
```
- StringBuilder эффективнее конкатенации строк в цикле
- Итерируем только до top (активные элементы)

#### 4. MyStack с композицией (src/practice5/MyStack/MyStack.java:15-25)

**Композиция:**
```java
public class MyStack<E> {
    ArrayList<E> list = new ArrayList<>();  // has-a отношение
}
```

**Методы делегируют работу ArrayList:**
```java
public void push(E o) {
    list.add(o);
}

public E pop() {
    return list.removeLast();  // Java 21+
}

public E peek() {
    return list.getLast();  // Peek смотрит на последний элемент!
}
```

#### 5. MyStackExtArrL с наследованием (src/practice5/MyStack/MyStackExtArrL.java:6-27)

**Наследование:**
```java
public class MyStackExtArrL<E> extends ArrayList<E> {  // is-a отношение
}
```

**Важно:**
- Использует все методы ArrayList
- В main используется removeLast() для стекового поведения

**Композиция vs Наследование:**
- **Композиция (MyStack)**: Лучше для стека, скрывает ненужные методы ArrayList
- **Наследование (MyStackExtArrL)**: Проще, но дает доступ ко всем методам ArrayList, что может нарушить инкапсуляцию стека

#### 6. Comparable с дженериками (src/practice5/ArraysAndCircle/Circle.java:10-12)

```java
public class Circle implements Comparable<Circle> {
    public int compareTo(Circle o) {
        return Integer.compare(this.radius, o.radius);
    }
}
```

**Поиск максимума (src/practice5/ArraysAndCircle/TestFindMaxCircle.java:7-15):**
```java
static int findMaxCircle(ArrayList<Circle> circles) {
    int maxIndex = 0;
    for (int i = 1; i < circles.size(); i++) {
        if (circles.get(i).compareTo(circles.get(maxIndex)) > 0) {
            maxIndex = i;
        }
    }
    return maxIndex;
}
```
- Правильный алгоритм: сравниваем каждый элемент с текущим максимумом
- Возвращаем индекс, а не значение

#### 7. Generic поиск максимума в 2D массиве (src/practice5/ArraysAndCircle/Test2DArrayMaxSearch.java)

**Generic метод с ограничением типа:**
```java
public static <T extends Comparable<? super T>> Result<T> max(T[][] arr) {
    // ...
}
```
- `T extends Comparable` - тип должен реализовывать Comparable
- `? super T` - wildcard, позволяет сравнение с родительским типом

### Вопросы, которые могут задать

**В:** Что такое дженерики и зачем они нужны?
**О:** Дженерики позволяют создавать классы, интерфейсы и методы с параметрами типа. Обеспечивают безопасность типов на этапе компиляции и избавляют от необходимости приведения типов.

**В:** Что означает `<E>` в объявлении класса?
**О:** `E` - параметр типа (type parameter). Это placeholder для конкретного типа, который будет указан при создании объекта. Например, `GenericStack<Integer>` - E заменяется на Integer.

**В:** В чем разница между композицией и наследованием?
**О:**
- **Композиция (has-a)**: MyStack содержит ArrayList. Более гибко, скрывает детали реализации.
- **Наследование (is-a)**: MyStackExtArrL является ArrayList. Проще, но менее гибко.

**В:** Почему StringBuilder лучше конкатенации строк?
**О:** В цикле конкатенация `str += "x"` создает новый объект String каждый раз. StringBuilder изменяет существующий буфер, что эффективнее по памяти и скорости.

**В:** Что такое LIFO?
**О:** Last In, First Out - последним пришел, первым вышел. Принцип работы стека: элемент, добавленный последним, удаляется первым.

**В:** Почему peek() использует getLast(), а не getFirst()?
**О:** В стеке вершина (top) - это последний добавленный элемент. ArrayList добавляет в конец (add), поэтому вершина стека - последний элемент списка.

### Как запустить и продемонстрировать

```bash
# Компиляция generic стеков
javac src/practice5/GenericStack/*.java

# Запуск
java -cp src practice5.GenericStack.GenericStack1
java -cp src practice5.GenericStack.GenericStack2

# Компиляция и запуск MyStack
javac src/practice5/MyStack/*.java
java -cp src practice5.MyStack.MyStack
java -cp src practice5.MyStack.MyStackExtArrL

# Компиляция и запуск работы с массивами
javac src/practice5/ArraysAndCircle/*.java
java -cp src practice5.ArraysAndCircle.DeleteDuplicatesAndLinearSearch
java -cp src practice5.ArraysAndCircle.TestFindMaxCircle
java -cp src practice5.ArraysAndCircle.Test2DArrayMaxSearch
```

### Демонстрация работы

**GenericStack1:**
1. Создание стека с начальным массивом
2. Push элементов (показать увеличение размера)
3. Pop элемента
4. Peek (просмотр без удаления)
5. isEmpty проверка

**MyStack:**
1. Ввод 5 строк
2. Вывод в обратном порядке (LIFO)

**DeleteDuplicatesAndLinearSearch:**
1. Создание списка с дубликатами
2. Удаление дубликатов
3. Поиск элемента

---

## Практика 6: Очередь. Паттерны проектирования

### Что демонстрирует эта практика

1. **Структура данных Очередь (Queue)** - FIFO
2. **Реализация стека через очереди**
3. **Порождающие паттерны проектирования**:
   - Singleton (Одиночка)
   - Factory Method (Фабричный метод)
   - Abstract Factory (Абстрактная фабрика)
   - Builder (Строитель)
   - Prototype (Прототип)

### Ключевые концепции для объяснения

#### 1. Stack on Queue (src/practice6/StackOnQueue/StackOnQueue.java)

**Задача:** Реализовать стек (LIFO) используя очереди (FIFO)

**Поля:**
```java
private Queue<E> myStack;
private Queue<E> copyStack;
```

**Метод push (простой):**
```java
public void push(E e) {
    myStack.add(e);  // Просто добавляем в очередь
}
```

**Метод pop (сложный, src/practice6/StackOnQueue/StackOnQueue.java:38-46):**
```java
public E pop() {
    if (myStack.isEmpty()) throw new NoSuchElementException("стек пуст");
    while (myStack.size() > 1) {
        copyStack.add(myStack.poll());  // Перемещаем все кроме последнего
    }
    E o = myStack.poll();  // Получаем последний элемент
    swapStacks();  // Меняем очереди местами
    return o;
}
```

**Как это работает:**
1. Все элементы кроме последнего перемещаются в copyStack
2. Последний элемент извлекается и возвращается
3. Очереди меняются местами (copyStack становится myStack)

**Метод top (просмотр):**
```java
public E top() {
    E lastObject = null;
    while (!myStack.isEmpty()) {
        lastObject = myStack.poll();
        copyStack.add(lastObject);
    }
    swapStacks();
    return lastObject;  // Возвращаем последний без удаления
}
```

#### 2. Singleton - AppSettings (src/practice6/DesignPatterns/Singleton/AppSettings.java:16-32)

**Цель паттерна:** Гарантировать, что у класса есть только один экземпляр, и предоставить глобальную точку доступа к нему.

**Реализация:**
```java
public class AppSettings {
    private static volatile AppSettings instance;
    private final Map<String, String> settings = new ConcurrentHashMap<>();

    private AppSettings() {}  // Приватный конструктор!

    public static AppSettings getInstance() {
        if (instance == null) {
            synchronized (AppSettings.class) {
                if (instance == null) {
                    instance = new AppSettings();
                }
            }
        }
        return instance;
    }
}
```

**Ключевые моменты:**
- **volatile** - гарантирует видимость изменений между потоками
- **synchronized** - защита от создания нескольких экземпляров в многопоточной среде
- **Double-checked locking** - проверка дважды для производительности
- **ConcurrentHashMap** - потокобезопасная Map

**Использование:**
```java
AppSettings settings = AppSettings.getInstance();
settings.setSetting("theme", "dark");
String theme = settings.getSetting("theme");
```

#### 3. Factory Method - DeliveryService (src/practice6/DesignPatterns/FactoryMethod/DeliveryService.java)

**Цель паттерна:** Определить интерфейс для создания объекта, но позволить подклассам решать, какой класс инстанцировать.

**Абстрактный класс:**
```java
abstract public class DeliveryService {
    abstract public void createOrder(Map<String, Integer> order);
}
```

**Конкретные реализации:**
```java
public class PizzaDelivery extends DeliveryService {
    @Override
    public void createOrder(Map<String, Integer> order) {
        setDeliveryName("Доставка пиццы");
        setOrder(order);
    }
}

public class GroceryDelivery extends DeliveryService {
    @Override
    public void createOrder(Map<String, Integer> order) {
        setDeliveryName("Доставка продуктов");
        setOrder(order);
    }
}
```

**Метод setOrder с валидацией (src/practice6/DesignPatterns/FactoryMethod/DeliveryService.java:26-38):**
```java
public void setOrder(Map<String, Integer> order) {
    notInStock.clear();
    this.order.clear();

    for (Map.Entry<String, Integer> entry : order.entrySet()) {
        if (stock.containsKey(entry.getKey())) {
            this.order.put(entry.getKey(), entry.getValue());
        } else {
            notInStock.add(entry.getKey());
        }
    }
    this.orderDate = LocalDateTime.now();
}
```

**Использование Map.Entry для эффективной итерации (src/practice6/DesignPatterns/FactoryMethod/DeliveryService.java:59-62):**
```java
for (Map.Entry<String, Integer> entry : order.entrySet()) {
    double price = entry.getValue() * stock.get(entry.getKey());
    sb.append(String.format("%-20s %-12d %-10.2f%n",
        entry.getKey(), entry.getValue(), price));
}
```

#### 4. Abstract Factory - GUI компоненты

**Цель паттерна:** Предоставить интерфейс для создания семейств связанных объектов без указания их конкретных классов.

**Интерфейс фабрики:**
```java
interface GUIFactory {
    Button createButton();
    Checkbox createCheckbox();
}
```

**Конкретные фабрики:**
```java
class WinGUIFactory implements GUIFactory {
    public Button createButton() { return new WinButton(); }
    public Checkbox createCheckbox() { return new WinCheckbox(); }
}

class MacGUIFactory implements GUIFactory {
    public Button createButton() { return new MacButton(); }
    public Checkbox createCheckbox() { return new MacCheckbox(); }
}
```

**Использование:**
```java
GUIFactory factory = new WinGUIFactory();
Button btn = factory.createButton();
Checkbox chk = factory.createCheckbox();
```

#### 5. Builder - Order

**Цель паттерна:** Отделить конструирование сложного объекта от его представления.

**Внутренний класс OrderBuilder:**
```java
public static class OrderBuilder {
    private String mainCourse;
    private String sideDish;
    private String drink;
    private String dessert;

    public OrderBuilder setMainCourse(String mainCourse) {
        this.mainCourse = mainCourse;
        return this;  // Возвращаем this для цепочки вызовов
    }

    public Order build() {
        return new Order(this);
    }
}
```

**Fluent interface (цепочка вызовов):**
```java
Order order = new Order.OrderBuilder()
    .setMainCourse("Стейк")
    .setDrink("Кола")
    .setDessert("Тирамису")
    .build();
```

#### 6. Prototype - Shape

**Цель паттерна:** Создавать новые объекты копированием существующих.

**Интерфейс:**
```java
public interface Shape {
    Shape clone();
}
```

**Реализация:**
```java
public class Circle implements Shape {
    private double radius;

    @Override
    public Shape clone() {
        return new Circle(radius);  // Создаем копию
    }
}
```

**Использование:**
```java
Circle original = new Circle(5.0);
Circle copy = (Circle) original.clone();
copy.setRadius(10.0);  // Изменение копии не влияет на оригинал
```

### Вопросы, которые могут задать

**В:** В чем разница между Stack и Queue?
**О:**
- **Stack (LIFO)**: Последним пришел - первым вышел. Как стопка тарелок.
- **Queue (FIFO)**: Первым пришел - первым вышел. Как очередь в магазине.

**В:** Зачем реализовывать стек через очереди?
**О:** Это демонстрирует понимание структур данных и умение реализовать одну через другую. Показывает алгоритмическое мышление.

**В:** Что такое паттерны проектирования?
**О:** Проверенные решения типовых задач проектирования. Описывают архитектуру взаимодействия классов и объектов.

**В:** Зачем нужен Singleton?
**О:** Когда нужен ровно один экземпляр класса в системе. Примеры: настройки приложения, пул соединений, логгер.

**В:** В чем преимущество Factory Method?
**О:** Отделяет логику создания объектов от их использования. Позволяет добавлять новые типы без изменения существующего кода.

**В:** Что такое double-checked locking в Singleton?
**О:** Оптимизация производительности: первая проверка без синхронизации (быстро), вторая внутри synchronized (для потокобезопасности).

**В:** Зачем volatile для instance в Singleton?
**О:** Гарантирует, что изменения instance видны всем потокам. Предотвращает проблемы с кешированием в многопоточной среде.

**В:** В чем отличие Abstract Factory от Factory Method?
**О:**
- **Factory Method**: Создает один продукт
- **Abstract Factory**: Создает семейство связанных продуктов (Button + Checkbox для одной ОС)

**В:** Зачем нужен Builder?
**О:** Для создания сложных объектов с множеством параметров. Делает код читаемым и позволяет создавать объекты пошагово.

**В:** В чем разница между клонированием и созданием нового объекта?
**О:** Клонирование копирует состояние существующего объекта, что может быть быстрее, чем создание с нуля, особенно для сложных объектов.

### Как запустить и продемонстрировать

```bash
# Stack on Queue
javac src/practice6/StackOnQueue/*.java
java -cp src practice6.StackOnQueue.TestStackOnQueue

# Singleton
javac src/practice6/DesignPatterns/Singleton/*.java
java -cp src practice6.DesignPatterns.Singleton.App

# Factory Method
javac src/practice6/DesignPatterns/FactoryMethod/*.java
java -cp src practice6.DesignPatterns.FactoryMethod.TestDelivery

# Abstract Factory
javac src/practice6/DesignPatterns/AbstractFactory/*.java
java -cp src practice6.DesignPatterns.AbstractFactory.App

# Builder
javac src/practice6/DesignPatterns/Builder/*.java
java -cp src practice6.DesignPatterns.Builder.App

# Prototype
javac src/practice6/DesignPatterns/Prototype/*.java
java -cp src practice6.DesignPatterns.Prototype.App
```

### Демонстрация работы

**StackOnQueue:**
1. Push двух элементов
2. Вызов top() (просмотр без удаления)
3. Вызов pop() (удаление)
4. Проверка empty()
5. Вывод содержимого

**Singleton:**
1. Создание нескольких "экземпляров" (getInstance)
2. Показать, что это один объект (изменения видны везде)
3. Добавление настроек
4. Получение настроек

**Factory Method:**
1. Создание PizzaDelivery
2. Создание GroceryDelivery
3. Создание заказов
4. Показать разный вывод для разных типов доставки

---

## Общие вопросы по всем трем практикам

### 1. Исключения

**В:** Какие виды исключений вы знаете?
**О:**
- **Checked**: Должны быть обработаны (Exception)
- **Unchecked**: Могут не обрабатываться (RuntimeException, Error)

**В:** Когда использовать throw, а когда throws?
**О:**
- **throw**: Для выбрасывания исключения внутри метода
- **throws**: Для объявления, что метод может выбросить исключение

### 2. Дженерики

**В:** Что такое type erasure?
**О:** Java удаляет информацию о типах на этапе компиляции. `ArrayList<String>` и `ArrayList<Integer>` в runtime - это просто `ArrayList`.

**В:** Можно ли создать массив дженериков?
**О:** Нельзя напрямую (`new E[10]`), но можно через приведение: `(E[]) new Object[10]`.

### 3. Паттерны проектирования

**В:** Какие категории паттернов существуют?
**О:**
- **Порождающие (Creational)**: Singleton, Factory Method, Abstract Factory, Builder, Prototype
- **Структурные (Structural)**: Adapter, Decorator, Facade и др.
- **Поведенческие (Behavioral)**: Observer, Strategy, Command и др.

**В:** Когда какой паттерн использовать?
**О:**
- **Singleton**: Нужен один экземпляр
- **Factory Method**: Создание объектов с различной логикой
- **Abstract Factory**: Создание семейств связанных объектов
- **Builder**: Сложные объекты с множеством параметров
- **Prototype**: Клонирование сложных объектов

### 4. Структуры данных

**В:** В чем разница между ArrayList и LinkedList?
**О:**
- **ArrayList**: Быстрый доступ по индексу O(1), медленная вставка в середину O(n)
- **LinkedList**: Медленный доступ O(n), быстрая вставка/удаление O(1)

**В:** Когда использовать Stack, а когда Queue?
**О:**
- **Stack**: Отмена действий (undo), обход в глубину, вычисление выражений
- **Queue**: Обработка задач по порядку, обход в ширину

---

## Практические советы для защиты

### Подготовка

1. **Запустите все программы** и убедитесь в их работоспособности
2. **Знайте паттерны наизусть** - структуру и назначение
3. **Понимайте разницу** между checked и unchecked исключениями
4. **Знайте, как работают дженерики** и зачем они нужны
5. **Подготовьте примеры** использования каждого паттерна

### Во время защиты

1. **Объясните задачу** своими словами
2. **Нарисуйте диаграммы** классов и паттернов
3. **Покажите работу** программы
4. **Объясните выбор** структур данных и паттернов
5. **Будьте готовы** к вопросам по теории

### Типичные ошибки

❌ "Это паттерн... забыл какой"
✅ Знайте все паттерны и их применение

❌ "Не помню, зачем здесь исключение"
✅ Объясните логику валидации

❌ "Дженерики - это как-то про типы"
✅ Четко объясните преимущества дженериков

---

## Диаграммы

### Иерархия исключений

```
Throwable
    ├── Error (системные ошибки)
    └── Exception
            ├── RuntimeException (unchecked)
            │       ├── IllegalArgumentException
            │       ├── ArrayIndexOutOfBoundsException
            │       └── NoSuchElementException
            └── Другие (checked)
                    └── IllegalTriangleException
```

### Паттерны проектирования

```
Порождающие паттерны:

Singleton:
    AppSettings
        └── instance: AppSettings (один экземпляр)

Factory Method:
    DeliveryService (abstract)
        ├── PizzaDelivery
        └── GroceryDelivery

Abstract Factory:
    GUIFactory (interface)
        ├── WinGUIFactory → WinButton, WinCheckbox
        └── MacGUIFactory → MacButton, MacCheckbox

Builder:
    Order
        └── OrderBuilder (внутренний класс)

Prototype:
    Shape (interface)
        ├── Circle (implements Shape, clone())
        └── Rectangle (implements Shape, clone())
```

---

## Команды для быстрого запуска

```bash
# Practice 4
javac src/practice4/figures/**/*.java && java -cp src practice4.figures.app.TestTriangle
javac src/practice4/secondPart/*.java && java -cp src practice4.secondPart.MonthsAndDays

# Practice 5
javac src/practice5/GenericStack/*.java && java -cp src practice5.GenericStack.GenericStack1
javac src/practice5/MyStack/*.java && java -cp src practice5.MyStack.MyStack
javac src/practice5/ArraysAndCircle/*.java && java -cp src practice5.ArraysAndCircle.TestFindMaxCircle

# Practice 6
javac src/practice6/StackOnQueue/*.java && java -cp src practice6.StackOnQueue.TestStackOnQueue
javac src/practice6/DesignPatterns/Singleton/*.java && java -cp src practice6.DesignPatterns.Singleton.App
javac src/practice6/DesignPatterns/FactoryMethod/*.java && java -cp src practice6.DesignPatterns.FactoryMethod.TestDelivery
```

---

## Финальный чек-лист перед защитой

### Практика 4
- [ ] Понимаю иерархию GeometricObject
- [ ] Могу объяснить Triangle и валидацию
- [ ] Знаю разницу между checked и unchecked исключениями
- [ ] Понимаю интерфейс Comparable
- [ ] Могу объяснить Colorable
- [ ] Знаю формулу Герона для площади треугольника

### Практика 5
- [ ] Понимаю, что такое дженерики и зачем они нужны
- [ ] Могу объяснить generic методы
- [ ] Знаю разницу между композицией и наследованием
- [ ] Понимаю, как работает стек (LIFO)
- [ ] Могу объяснить StringBuilder vs String конкатенация
- [ ] Понимаю работу с Comparable и generics

### Практика 6
- [ ] Понимаю разницу между Stack и Queue
- [ ] Могу объяснить реализацию стека через очереди
- [ ] Знаю все 5 порождающих паттернов
- [ ] Могу нарисовать UML диаграмму для каждого паттерна
- [ ] Понимаю double-checked locking в Singleton
- [ ] Знаю, когда использовать каждый паттерн

---

**Удачи на защите! 🚀**

Ключ к успеху: понимание концепций, а не заучивание кода. Если вы понимаете ЗАЧЕМ используется каждый паттерн и структура данных, защита пройдет легко.
